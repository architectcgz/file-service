package com.platform.fileservice.contract.upload.model;

/**
 * Upload mode values visible in the v2 contract.
 */
public enum UploadModeView {
    AUTO,
    INLINE,
    DIRECT,
    PRESIGNED_SINGLE
}
