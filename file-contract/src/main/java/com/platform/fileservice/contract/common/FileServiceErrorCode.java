package com.platform.fileservice.contract.common;

/**
 * Stable integration error codes for the v2 contract.
 */
public enum FileServiceErrorCode {
    UNAUTHORIZED,
    ACCESS_DENIED,
    FILE_NOT_FOUND,
    TENANT_NOT_FOUND,
    VALIDATION_ERROR,
    UPLOAD_SESSION_EXPIRED,
    STORAGE_FAILURE
}
